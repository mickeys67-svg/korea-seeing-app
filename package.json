{
    "name": "korea-sky-seeing-app",
    "version": "1.0.0",
    "description": "Korea Sky Seeing & Weather App",
    "main": "backend/server.js",
    "scripts": {
        "start": "cd backend && npm start",
        "server": "cd backend && npm start",
        "client": "cd frontend && npm run dev",
        "build": "cd frontend && npm install && npm run build",
        "render-build": "npm install && cd backend && npm install && cd ../frontend && npm install && npm run build",
        "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
        "deploy": "npm run install-all && npm run build && npm start"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "mongodb": "^7.0.0"
    }
}
